# 振り返り(開発課題ログ)

このファイルは `self-evolving-ai` 自体の開発中に実際に発生した課題と解決策を記録する。
日々の「自己発展の振り返り」は `context/reflections/YYYY-MM-DD.md` 側に記録する
(本ファイルは開発メタの振り返り、あちらは運用開始後の自己発展の振り返り、と役割を分けている)。

## Phase 1 (2026-07-28)

- 課題: 「ツール非依存」を素朴に実現しようとすると、つい特定ツールのスキル形式
  (例: Claude CodeのSKILL.md構造)に寄せてしまいがちだった。
  解決策: 「ファイルの読み書き」と「標準ライブラリのみのPython実行」という、
  どのAIエージェントでも確実に持っている最小公倍数の操作だけに絞って設計した。
- 課題: 判断基準の自動更新(アイデア1)を素朴に実装すると、生成AIの誤判断が
  そのまま基準に混入するリスクがある。
  解決策: 生成(PROPOSED.md作成)と反映(CURRENT.md更新)を別スクリプトに分離し、
  人間承認を挟む2段階構成にした。
- 課題: 判断基準のバージョン管理(アイデア6)を独自のバージョニング機構で
  作ろうとすると過剰実装になる。
  解決策: gitのコミット履歴をそのままバージョン管理として使い、承認直前の
  スナップショットだけ `history/` に追加で退避する軽量な設計にとどめた。

## Phase 2 (2026-07-28 深夜)

- 課題(アイデア2): エラーと解決策の対応関係を自動で厳密に紐付けようとすると、
  イベントの記述だけでは判断が難しく、誤った対応付けをしてしまうリスクがある。
  解決策: 完全自動の対応付けはせず、「同日以降のsuccessイベント」を
  “対策の手がかり候補”として参考併記するだけにとどめ、実際の対応関係の確認は
  人間(または後続のAI)に委ねる設計にした。
- 動作確認: `append_event.py` でerror/success/todoイベントを記録 →
  `generate_reflection.py --date <日付>` で
  「今日やったこと/発生した課題と対策/次回に向けて」の3見出しが自動生成される
  ことを確認。5件ごとの見直しファイル生成ロジックも実装(閾値到達時に
  `context/reflections/_review/REVIEW_<n>.md` を生成)。テスト用の生成物は
  クリーンな状態に戻した。
- 課題(アイデア4): 日本語テキストは空白で分かち書きされないため、
  `re.findall(r"\S+", text)` による単語数カウントが文全体を1語として
  誤判定してしまった(短文チェックが機能しない)。
  解決策: 単語数ではなく空白除去後の文字数で短文判定するよう修正し、
  テストで日本語文でも正しくOK/WARNが出ることを確認した。
- 動作確認(アイデア8): `log_experiment.py` で実験結果を記録すると、
  `context/experiments/log.jsonl`(追記ログ)と
  `context/experiments/<日付>_<実験名>.md`(詳細メモ)の両方が生成されることを
  確認。このセルフテスト自体が実質的に最初の実験記録になったため、削除せず
  そのまま残した。
- 設計判断(アイデア3): 実際の`~/.claude/skills`のような外部ディレクトリを
  自動で操作するのは事故のリスクが大きいため、既定を必ずdry-run(レポートのみ)
  にし、実際に移動するには明示的に `--apply` を要求する設計にした。また
  `--skills-dir` にデフォルト値を持たせず、対象ディレクトリを毎回明示させる
  ことで誤爆を防いだ。動作確認は`/tmp`上のフィクスチャ(このセッションの
  一時ファイル)で実施し、テスト後に削除・使用ログもクリアした。
- 動作確認(アイデア5): 実際の `C:\Users\<user>\AI開発\CLAUDE.md` を対象に
  `agent_md_diagnostic.py` を実行し、行数(76行、上限200行以内)・見出し構成・
  MCP言及数を読み取り専用で棚卸しできることを確認した。対象ファイルへの変更は
  一切行っていない(レポート生成のみ)。生成されたレポートは実運用の最初の1件
  として `context/reports/` にそのまま残した。

## Phase 2.5 (2026-07-29): 評価役(Critic)の追加

- 設計判断: 「実行役/評価役/省察・記憶役/長期記憶/メタ司令役」という5役構成の
  提案を検討した結果、現状の仕組みは実行役の自己申告(`decision`/`error`
  イベント)がそのまま省察役の材料になっており、客観的にチェックする評価役が
  欠けていることが分かった。長期記憶のベクトルDB化は現状のテキスト量では
  過剰実装(かつ外部API依存が増えて「ツール非依存」の設計思想と衝突する)と
  判断し、評価役の追加のみを既存のプレーンファイル構成に組み込む形で進めた。
- 実装: `scripts/log_critic_finding.py` を新設し、`context/critic_findings.jsonl`
  に評価対象・指摘内容・重大度(low/medium/high)・判定(pass/fail)を記録できる
  ようにした。既存の `regenerate_criteria.py`(判断基準候補生成)と
  `generate_reflection.py`(振り返り自動生成)の両方を拡張し、
  `severity: high` かつ `verdict: fail` の指摘を、実行役の自己申告とは別枠の
  「評価役の重大な指摘」として取り込むようにした。
- 動作確認: `log_critic_finding.py` でテスト指摘を1件記録 →
  `regenerate_criteria.py` のPROPOSED.mdに「評価役(Critic)の重大な指摘」
  セクションが生成されること、`generate_reflection.py` の「発生した課題と対策」
  にも同じ指摘が反映されることを確認した。テスト用のPROPOSED.md/振り返りファイル
  は削除し、`critic_findings.jsonl`のテスト記録は最初の実データとしてそのまま
  残した。

## Phase 3 (2026-07-29〜30): 定期報告の自動スケジューリング

- 課題: claude.aiのルーティン機能でself-evolving-aiリポジトリを対象にすると
  最初は403(アクセス権無し)エラーになった。原因はClaude用GitHub Appが
  OAuth認証はされていたが、どのアカウントにもインストールされていなかったこと。
  解決策: `https://github.com/apps/claude/installations/new` から
  「Only select repositories」でself-evolving-aiを明示的に選択しインストール。
  GitHub側の2段階認証(sudo mode)はGitHub Mobileの承認がタイムアウトしたため、
  メールコード認証に切り替えて完了した。
- 設計判断: 判断基準の自動反映(`approve_criteria.py`)やスキル移動
  (`skill_triage.py --apply`)は、定期実行のプロンプトから明示的に
  「絶対に実行しないこと」と制約を書き、自動化しても人間承認ゲートが
  壊れないようにした。
- 設計判断(Discord通知): Webhook URLはリポジトリにもスクリプト
  (`scripts/notify_discord.py`)にもハードコードせず、`--webhook-url`引数or
  環境変数で渡す設計にした。既存のAI開発プロジェクト(SNS投稿通知チャンネル)の
  Webhookを最初試したが、通知が混ざるとノイズになるためユーザーの希望で
  self-evolving-ai専用のDiscordチャンネル・Webhookを新規発行して分離した。
- 動作確認: Discord Webhookへのテスト送信は初回403(原因不明、User-Agent
  ヘッダー追加で解消)、専用チャンネルのWebhookでは1回目から成功(status 204)。
  最終的にルーティンのプロンプトにDiscord通知ステップ(手順6)を追加し、
  GitHub Issue作成と同じ内容を専用チャンネルに通知するよう更新した。
