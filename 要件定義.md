# 自己発展型AI基盤 (self-evolving-ai) 要件定義

## 概要

Claude Code / GPT Codex など、AIツールの種類に関わらず「同じ場所に同じ形式で
コンテキストが蓄積される」ことを目指す基盤。開発ログ・振り返り・判断基準を
プレーンなファイル(Markdown/JSONL)としてgit管理し、どのAIツールからも
同じ手順で読み書きできるようにする。最終目標は、AI自身が自己発展の状況を
定期的にユーザーへ振り返り報告する「自己発展型AI」であること。

## 役割構成(5役モデルとのマッピング)

「実行役/評価役/省察・記憶役/長期記憶/メタ司令役」という5役構成の考え方を
取り入れつつ、現状の規模ではベクトルDB等の重い仕組みは導入せず、既存のプレーン
ファイル構成に役割をマッピングする形にとどめている(段階導入)。

| 役割 | 対応するもの | 備考 |
|---|---|---|
| 実行役(Generator) | タスクを実行するAIエージェント自身(Claude Code / GPT Codex 等) | 特定の実装物ではない |
| 評価役(Critic) | `scripts/log_critic_finding.py` + `context/critic_findings.jsonl` | 実行役の自己申告に頼らず、独立した視点で成果物を検証し指摘を記録する。LLM呼び出しはせず記録に徹する軽量実装 |
| 省察・記憶役(Reflector) | `scripts/generate_reflection.py` | イベント/Critic指摘から「なぜ失敗したか」を振り返りとしてまとめる |
| 長期記憶(Long-Term Memory) | `context/criteria/CURRENT.md` + `context/events.jsonl` + `context/critic_findings.jsonl` | ベクトルDBではなくプレーンテキスト。量が増えて破綻するまではこの形式を維持する方針 |
| メタ司令役(Meta-Learner) | `scripts/regenerate_criteria.py` → `scripts/approve_criteria.py` | 蓄積された記録から判断基準候補を生成し、人間承認を経てCURRENT.mdを更新する |

評価役を独立させた理由: 実行役が生成したものを実行役自身の申告(`decision`/`error`
イベント)だけで判断すると、都合の悪い不備を見落とすリスクがある。評価役の指摘
(`severity: high` かつ `verdict: fail`)は `regenerate_criteria.py` /
`generate_reflection.py` の双方で、実行役の自己申告とは別枠の材料として扱われる。

長期記憶をベクトルDB化しなかった理由: 現状のテキスト量は`CURRENT.md`を直接AIに
読ませれば十分機能する規模であり、embedding APIやベクトルDBを導入すると外部
サービス依存が増え「ツール非依存」の設計思想と衝突する。テキスト量が増えて
プロンプトに載せきれなくなった時点で改めて検討する。

## コア要件(ツール非依存の共有コンテキストストア)

- 特定ツールのAPI・スキルフォーマット・MCPに依存しない。
- 読み書きは「ファイルをそのまま読む/書く」「標準ライブラリのみのPythonスクリプトを
  `python scripts/xxx.py` で実行する」の2種類の操作だけで完結する。
- すべてgitで差分管理できるテキスト形式(Markdown / JSON Lines)を使う。

### ディレクトリ構成

```
self-evolving-ai/
├── context/
│   ├── events.jsonl              # 追記専用の生イベントログ(全AIツール共通)
│   ├── critic_findings.jsonl     # 評価役(Critic)の指摘ログ(log_critic_finding.pyが追記)
│   ├── skill_usage.jsonl         # スキル/MCP使用ログ(log_skill_usage.pyが追記、アイデア3)
│   ├── criteria/
│   │   ├── CURRENT.md            # 現在有効な判断基準(正本)
│   │   ├── PROPOSED.md           # 自動生成された次期候補(regenerate_criteria.pyが生成)
│   │   └── history/              # 承認前の旧バージョンのタイムスタンプ付き退避先
│   ├── reflections/
│   │   ├── YYYY-MM-DD.md         # 日次(将来的に間隔変更予定)の振り返りエントリ
│   │   └── _review/
│   │       └── REVIEW_<n>.md     # 振り返りが5件たまるごとに自動生成される見直し一覧(アイデア2)
│   ├── experiments/
│   │   ├── log.jsonl             # 実験の仮説/結果/採否ログ(アイデア8)
│   │   └── <日付>_<実験名>.md     # 実験ごとの詳細メモ
│   └── reports/
│       ├── skill_triage_<日付>.md        # スキル/MCP棚卸しレポート(アイデア3)
│       └── agent_md_diagnostic_<日付>.md # AGENT.MD自己診断レポート(アイデア5)
├── scripts/
│   ├── append_event.py           # イベントをevents.jsonlに追記する共通コマンド
│   ├── backup_run.py             # バックアップ一式の入口。棚卸し→同期→検証を順に実行(Phase 4)
│   ├── backup_manifest.py        # バックアップ対象の棚卸し・manifest生成(Phase 4-A、読み取り専用)
│   ├── backup_sync.py            # manifestに従いバックアップ先へコピー(Phase 4-B、既定dry-run)
│   ├── backup_verify.py          # バックアップ先を再ハッシュし復元可能性を検証(Phase 4-B)
│   ├── backup_restore.py         # バックアップから別ディレクトリへ復元+照合(Phase 4-D)
│   ├── register_backup_task.ps1  # 定期実行をWindowsタスクスケジューラへ登録(Phase 4-D)
│   ├── log_critic_finding.py     # 評価役(Critic)の指摘をcritic_findings.jsonlに追記
│   ├── regenerate_criteria.py    # イベント/振り返り/Critic指摘から判断基準候補を生成(アイデア1)
│   ├── approve_criteria.py       # 候補を承認しCURRENT.mdへ反映+旧版退避(アイデア1・6)
│   ├── generate_reflection.py    # イベントから振り返りを自動生成+5件ごとの見直し生成(アイデア2)
│   ├── prompt_self_review.py     # 目的/手段/アウトプットの3点セットを軽量チェック(アイデア4)
│   ├── log_experiment.py         # 実験の仮説/結果/採否を記録(アイデア8)
│   ├── log_skill_usage.py        # スキル/MCP使用ログの追記(アイデア3)
│   ├── skill_triage.py           # 使用頻度が低いスキル/MCPを棚卸し・2軍へ退避(アイデア3)
│   └── agent_md_diagnostic.py    # CLAUDE.md/AGENT.MDの読み取り専用診断レポート(アイデア5)
├── 要件定義.md
├── README.md
├── 振り返り.md
├── コンテキストバックアップ設計.md   # ローカルPCコンテキストのバックアップ方式検討(Phase 4)
├── 復元手順.md                      # バックアップからの復元手順(Phase 4-D)
└── .gitignore
```

### ファイルフォーマットのルール

- `context/events.jsonl`: 1行1JSON。追記専用(既存行は変更しない)。
  必須キー: `date`(YYYY-MM-DD, 呼び出し側が明示的に指定。スクリプト内で
  現在時刻を自動取得しない), `actor`(例: `claude-code` / `gpt-codex` / `human`),
  `type`(`decision` / `error` / `success` / `todo` / `milestone` など),
  `summary`(1〜2文)。
- `context/criteria/CURRENT.md`: 判断基準の正本。直接編集も可だが、
  自動更新ループ経由の場合は `approve_criteria.py` を通すこと。
- `context/reflections/YYYY-MM-DD.md`: その日の振り返り。
  「今日やったこと」「発生した課題と対策」「次回に向けて」の3見出しを推奨。
  `generate_reflection.py` で自動生成も可能(手動編集との併用も可)。
- `context/critic_findings.jsonl`: 1行1JSON。`log_critic_finding.py` が追記。
  必須キー: `date`, `actor`, `artifact`(評価対象), `finding`(指摘内容),
  `severity`(`low`/`medium`/`high`), `verdict`(`pass`/`fail`)。
- `context/skill_usage.jsonl`: 1行1JSON。`log_skill_usage.py` が追記。
  必須キー: `date`, `actor`, `skill_name`。
- `context/experiments/log.jsonl`: 1行1JSON。`log_experiment.py` が追記。
  必須キー: `date`, `name`, `hypothesis`, `result`, `verdict`, `note`。
- `context/reports/`: 各種診断・棚卸しスクリプトが生成するレポート(読み取り専用の
  棚卸し結果。人間/AIが判断材料として読むためのファイルで、生成元のスクリプトが
  上書き生成する想定)。

### 読み書きルール(AIツール共通)

1. タスク開始時に `context/criteria/CURRENT.md` を読み、判断基準に従う。
2. タスク中に重要な決定・エラー・成功パターンがあれば
   `python scripts/append_event.py --date <今日の日付> --actor <自分のツール名> --type <種別> --summary "..."`
   で追記する。
2.5. 成果物ができたら、実行役とは別の視点(可能なら別セッション/別レビュー)で
   批判的に検証し、`python scripts/log_critic_finding.py` で指摘を記録する
   (評価役のステップ。実行役の自己申告だけに頼らないための工程)。
3. タスク完了時、必要に応じて `context/reflections/YYYY-MM-DD.md` を作成・追記する。
4. 判断基準の見直しは `python scripts/regenerate_criteria.py` →
   人間が `context/criteria/PROPOSED.md` を確認 →
   `python scripts/approve_criteria.py` で承認、の順に行う。
5. タスク開始前に `python scripts/prompt_self_review.py` で指示文に
   目的・手段・アウトプットが揃っているか軽くセルフチェックする(任意)。
6. 新しい手法を小さく試したら `python scripts/log_experiment.py` で
   仮説・結果・採否を記録する。
7. スキル/MCPを使ったら `python scripts/log_skill_usage.py` で使用ログを残し、
   棚卸しのタイミングで `python scripts/skill_triage.py` を実行する
   (既定はレポートのみ。実際に整理するときだけ `--apply` を付ける)。
8. CLAUDE.md/AGENT.MD相当のファイルの棚卸しは
   `python scripts/agent_md_diagnostic.py` で行う(読み取り専用、対象ファイルは変更しない)。

具体的な使い方(コマンド例)は [README.md](./README.md) を参照。

## 現状サマリー(2026-07-29時点)

- Phase 1(共有コンテキストストア基盤 + 判断基準の自動更新ループ + バージョン管理)完了。
- Phase 2は8アイデア中7つ(1・2・3・4・5・6・8)実装・動作確認済み。
- アイデア7(外部トレンド自動トリアージ)のみ、外部API/認証情報が必要になる
  可能性が高いため保留中(詳細は下記TODO参照)。
- Phase 2.5: 5役モデル(実行役/評価役/省察・記憶役/長期記憶/メタ司令役)に
  照らして不足していた評価役(Critic)を追加。`log_critic_finding.py` で記録し、
  `regenerate_criteria.py`・`generate_reflection.py` の両方に統合済み。

## Phase 1 (実装済み・MVP)

- [x] GitHubリポジトリ作成・共有コンテキストストアのディレクトリ/フォーマット設計
- [x] アイデア1: 判断基準ファイルの自動更新ループ
      (`regenerate_criteria.py` で候補生成 → 人間承認 → `approve_criteria.py` で反映)
- [x] アイデア6: 判断基準のバージョン管理
      (gitコミット履歴 + `context/criteria/history/` への旧版退避)
- [x] 最初の振り返りエントリ(`context/reflections/2026-07-28.md`)

## Phase 2 (実装済み・アイデア7を除き完了)

| # | アイデア | 概要 | 状態 |
|---|---|---|---|
| 2 | 振り返り自動生成の拡張 | エラーログ・実行差分から「課題と対策」を自動追記。5回ごとの見直しルールと連動 | [x] 実装済み(`scripts/generate_reflection.py`) |
| 4 | プロンプト自己レビュー層 | 「目的・手段・アウトプット」の3点セットが揃っているかを実行前チェック | [x] 実装済み(`scripts/prompt_self_review.py`) |
| 8 | 小さく試して採否を記録する実験フロー | 新技術をサンドボックスで試し、成功/失敗を自己改善ログに蓄積 | [x] 実装済み(`scripts/log_experiment.py`) |
| 3 | スキル/MCP自己整理スクリプト | 使用頻度ログを取り、閾値以下のスキルを2軍フォルダへ自動移動 | [x] 実装済み(`scripts/skill_triage.py`。既定dry-run、`--apply`で実移動) |
| 5 | AGENT.MD自己診断レポート | 週次で行数・不要MCP・スキル発動履歴を棚卸し(レポートのみ、自動変更なし) | [x] 実装済み(`scripts/agent_md_diagnostic.py`。読み取り専用) |
| 7 | 外部トレンド自動トリアージ | 新しいAI活用テクニックを自動収集し、自分の開発フローへの取り込み可否を判定 | 未着手(外部API/認証情報が必要になる可能性が高く要ユーザー確認) |

## Phase 3 (実装済み): 定期報告の自動スケジューリング

「常に頼れるシステムにしたい」という方針のもと、定期報告・判断基準更新ループを
claude.aiの「ルーティン」機能(クラウドagent、cron実行)で自動化した。

- **スケジュール**: 毎日 0:00 UTC(9:00 JST)
- **実行内容**: `generate_reflection.py` → `regenerate_criteria.py` → 変更があれば
  commit&push → その日の要約をGitHub Issue作成 + Discord通知
  (`scripts/notify_discord.py`、専用チャンネルのWebhookを使用)
- **安全策**: `approve_criteria.py`(CURRENT.mdへの反映)はルーティンから
  絶対に実行しない。`skill_triage.py --apply`等のファイル移動・削除を伴う
  スクリプトも実行しない。エラー時は自己判断で仕様を変えず、エラー内容を
  そのまま報告する制約をプロンプトに明記している。
- **設定の保管場所**: ルーティンの定義(cron式・プロンプト・Webhook URL含む)は
  claude.ai側で管理しており、このリポジトリには含まれない(秘密情報を
  gitにコミットしないため)。

### TODO: アイデア7(外部トレンド自動トリアージ)

新しいAI活用テクニックの自動収集には、外部サービス(RSS/検索API/SNS API等)への
アクセスが必要になる可能性が高い。認証情報(APIキー等)の新規取得が必要になった
時点で、運用ルール上ユーザーへの確認が必須(CLAUDE.mdの確認必須4カテゴリの
「APIキー・認証情報」に該当)。そのため今回は実装を見送り、次のいずれかが
決まった時点で着手する:

- 既存のWebSearch/WebFetch等、既にAI側が持っているツールで収集し、
  判定ロジック(取り込むべきか)だけをこのリポジトリのスクリプトとして
  実装する(=新規API/認証情報が不要な範囲に限定する)方針にするか、
- 新規API利用(RSSリーダーAPI・専用トレンド収集サービス等)をユーザーに
  提案し、認証情報取得の可否を確認してから実装するか。

## Phase 4 (フェーズA・B・D完了): ローカルPCコンテキストのバックアップ

`context/` 配下はgit + GitHubで多重化されているが、`CLAUDE.md`・スキル定義・
ノート等、ローカルPCにしか存在しないコンテキストは消失リスクが残っている。
また外出先(スマホ)から参照できない。

方式比較・アーキテクチャ・スクリプト構成・段階導入計画は
[コンテキストバックアップ設計.md](./コンテキストバックアップ設計.md) にまとめた。要点のみ:

- 実データはクラウドストレージ、メタデータ/タグはNotionのラベルDB、
  同期処理は標準ライブラリのみのPython CLIという3層構成を採用する。
- スマホからは閲覧(Read-Only)に割り切ることで、モバイル側のファイル競合を排除する。
- Obsidian Sync等のE2E暗号化された専用同期は、クラウド上のデータをAIに読ませられず
  「AIが読めるコンテキスト置き場」という目的と衝突するため採用しない。
- クラウドストレージはGoogle Driveに決定。マイドライブの使用量が大きかったため、
  ミラーリング(クラウド→ローカル)ではなく「パソコン」区画(ローカル→クラウド)を使う。
  ローカルにバックアップ先を作り、そのフォルダをDriveへ上げる構成。
- Obsidianは現時点では導入しない。Vaultは単なるMarkdownフォルダなので、
  必要になった時点で既存フォルダをVaultとして開けばよく、移行コストが無い。
- フェーズA(`scripts/backup_manifest.py` / 走査・Tier判定・除外・manifest生成)と
  フェーズB(`scripts/backup_sync.py` + `scripts/backup_verify.py` / コピーと照合)は
  実装済み。同期は既定dry-run、削除は一切行わない。
- Notion連携(フェーズC)はAPIキーが必要なため保留。保留中でもフェーズA・B・Dだけで
  「消失リスクの解消」は成立する(フェーズCが担うのはスマホからの参照性のみ)。
- 2026-08-03、実PCで初回実行・検証まで完了(1433件 / 24.9MB、全件一致)。
  正本(ローカル)/ ローカル複製 / クラウド複製の3コピーが揃った。

残る未確定事項(対象パスの確定、同期先パス、Notion連携の可否、実行頻度)は
設計ドキュメントの「未確定事項」節を参照。

## 定期報告

- 最初の1週間: ディスパッチ(このセッション経由)で日次報告。
- 1週間後: 報告間隔(日次/週次など)と報告手段(Discord/Notion/その他)をユーザーと決定する。

## 成功条件

### Phase 1

- [x] Claude Code / GPT Codex のどちらから操作しても、同じ `context/` ディレクトリの
      同じファイル形式でコンテキストを読み書きできる。
- [x] `append_event.py` → `regenerate_criteria.py` → `approve_criteria.py` の一連の
      流れが実際に動作し、CURRENT.mdの更新がgit履歴として追跡できる。
- [x] 初回振り返りエントリが作成されている。

### Phase 2

- [x] アイデア2・4・8・3・5がそれぞれ動くMVPとして実装され、実データ
      (実際のイベント/実際のCLAUDE.md等)で動作確認済み。
- [x] 危険度の高い操作(アイデア3のスキル移動)は既定dry-run + 明示的な
      `--apply` でしか実行されない安全設計になっている。
- [ ] アイデア7は要件・外部連携方針が未確定のため保留(TODO参照)。
