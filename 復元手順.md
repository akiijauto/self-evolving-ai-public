# バックアップからの復元手順

PC故障・OS再インストール・誤削除の後に、バックアップからコンテキストを取り戻すための手順。

前提: [コンテキストバックアップ設計.md](./コンテキストバックアップ設計.md) の構成
(正本=ローカルPC / 複製=`C:\context-backup` / クラウド複製=Google Driveの「マイ パソコン」)。

**このファイルが正本です。** Notionの
[💾 コンテキストバックアップ・復元手順](https://app.notion.com/p/3b124f4cf1af8130a571e927d20416be)
にも同じ内容を置いてあるが、あちらは複製(スマホから参照するため)。
手順を変えるときはこのファイルを直し、Notion側に反映すること。

## 0. 原則

- **復元は正本の場所へ直接書き戻さない。** 別のディレクトリへ展開し、中身を確認してから
  人間が移す。`backup_restore.py` は正本のパスへは絶対に書き込まない。
- **バックアップ先を編集しない。** 復元元として使うだけ。

## 1. どのケースか判断する

| 状況 | バックアップの所在 | 進む先 |
|---|---|---|
| ファイルを消した / 壊した(PCは無事) | `C:\context-backup` にある | ケースA |
| PCが壊れた / 新しいPCに移行する | Google Driveにだけある | ケースB |

## ケースA: 同じPCで一部を取り戻す

```powershell
cd C:\Users\<user>\self-evolving-ai

# 1) 何が復元されるか確認(書き込みなし)
python scripts/backup_restore.py --date <バックアップした日> `
  --dest "C:/context-backup" --to "C:/restore-work"

# 2) 実行(復元後にsha256照合まで自動で走る)
python scripts/backup_restore.py --date <バックアップした日> `
  --dest "C:/context-backup" --to "C:/restore-work" --apply
```

`C:\restore-work` に `<ラベル>/<相対パス>` の形で展開される。
中身を確認し、必要なファイルだけ元の場所へ戻す。

一部だけでよいときはラベルで絞る。

```powershell
python scripts/backup_restore.py --date <日付> `
  --dest "C:/context-backup" --to "C:/restore-work" --only AI開発 --apply
```

`判定: **復元成功**(全件一致)` が出れば、展開したファイルはバックアップ時点と
バイト単位で同一。

## ケースB: 新しいPCへ移行する

**1. Google Driveからバックアップを取り戻す**

Google Drive for desktop を新PCにインストールし、同じアカウントでログイン。
Web版のドライブで **パソコン > マイ パソコン > context-backup** を開き、
フォルダごとダウンロードして `C:\context-backup` に展開する
(または新PCでも「マイ パソコン」同期を設定して降ろす)。

**2. リポジトリを取得する**

```powershell
git clone https://github.com/akiijauto/self-evolving-ai-public.git
cd self-evolving-ai
```

**3. manifestを用意する**

manifestは `context/backup/` にあり `.gitignore` 対象なので、cloneには含まれない。
バックアップ先の `_backup_state.json` に「前回書き込んだ内容のハッシュ」が
記録されているが、復元には元のmanifestがある方が確実。

manifestが手元にない場合は、`--manifest` を使わずバックアップ先を丸ごとコピーする。

```powershell
# manifestがある場合(照合付きで復元)
python scripts/backup_restore.py --date <日付> `
  --dest "C:/context-backup" --to "C:/restore-work" --apply

# manifestが無い場合(照合なし。エクスプローラーでのコピーと同等)
Copy-Item -Recurse "C:\context-backup\*" "C:\restore-work\"
```

**4. 元の配置へ戻す**

`<ラベル>` はバックアップ時の対象パスの末尾名に対応する。

| ラベル | 元の場所 |
|---|---|
| `AI開発/` | `C:\Users\<ユーザー名>\AI開発\` |
| `claude/` | `C:\Users\<ユーザー名>\.claude\`(settings.json 等) |
| `plugins/` | `C:\Users\<ユーザー名>\.claude\plugins\` |
| `codex/` | `C:\Users\<ユーザー名>\.codex\` |

バックアップ先のルートにある `_このフォルダはバックアップです.md` に、
**バックアップ時点の正本の絶対パス**が記録されているので、これを見れば対応が分かる。

**5. 除外していたものを入れ直す**

バックアップに含まれていないものがある。これらは復元ではなく再取得する。

- **認証情報**(`.env` / APIキー / 二段階認証のリカバリコード)
  → 意図的に除外している。パスワードマネージャー等から入れ直す。
- **プラグイン / スキルのキャッシュ**
  → マーケットプレイスから再インストールする。
- **実行ファイル・モデルデータ**(`*.exe` / `*.traineddata` 等)
  → 配布元から再ダウンロードする。
- **git管理されているリポジトリ**
  → `git clone` で取得する(バックアップにも入っているが、gitの方が新しい可能性がある)。

## 2. 復元訓練(年1回程度)

バックアップは「取れているか」ではなく「戻せるか」で価値が決まる。
実際に戻せることを確認しておく。

```powershell
# 別ディレクトリへ全件復元して、照合が通ることを確認する
python scripts/backup_restore.py --date <最新のバックアップ日> `
  --dest "C:/context-backup" --to "C:/restore-drill" --apply

# 確認できたら消す
Remove-Item -Recurse -Force "C:\restore-drill"
```

`判定: **復元成功**(全件一致)` と、想定したファイルが実際に開けることを確認する。
異常があれば `backup_run.py --apply` を実行し直す。

## 3. 復元できないもの(既知の限界)

- **バックアップ後に作ったファイル**は当然入っていない。最後に `backup_run.py --apply` を
  実行した時点までしか戻せない。定期実行を登録しておくと差が小さくなる
  (`scripts/register_backup_task.ps1`)。
- **50MBを超えるファイル**は既定で除外される(manifestに「除外した」記録だけ残る)。
- **ファイルの所有者・ACL等のメタデータ**は復元しない。中身とタイムスタンプのみ。
