# 引き継ぎ (需要データ収集システム)

作成日: 2026-08-03 / 作成: Claude Code (クラウドセッション)
引き継ぎ先: ローカルPCの Claude Code

このドキュメントは、クラウド上のセッションで進めた「需要データ収集システム」の
作業を、ローカルPCで引き継ぐためのもの。**プログラムの変更は不要で、
そのままPCから続きを進められる状態**になっている。

---

## 1. このシステムは何か

世界中の「何が売れているか」「何が求められているか」を、**各サイトの規約を
守った手段でのみ**収集し、ランキング形式に正規化して Notion に蓄積する。

設計の中心は「規約遵守をコードで強制する」こと。規約確認を人間の心がけに
委ねると収集元が増えたときに必ず抜けるため、**規約確認の証跡が
`demand/sources.json` に残っている収集元しか実行できない**構造にしてある。

詳しい設計思想は [README.md](./README.md) の「需要データ収集システム」節を参照。

---

## 2. 現状(2026-08-03時点)

| 項目 | 状態 |
|---|---|
| 収集元(既定で動くもの) | **10件** |
| 1日あたりの収集件数 | **1,633件** |
| レジストリ登録数 | **81件**(承認10 / 却下40 / 承認待ち30 / 不採用1) |
| テスト | **40件、全て通過** |
| 実データ | `context/demand/2026-08-02.jsonl`(207件)、`2026-08-03.jsonl`(1,633件) |
| Notion | 親ページと2日分の子ページを作成済み(自動反映は未接続) |
| ブランチ | `claude/global-demand-data-system-1z8z66` |
| PR | #8 (draft、未マージ) |

### 動いている収集元

| 収集元 | 取得内容 | 認証 |
|---|---|---|
| `steam` | **売上金額ベース**の週間トップ + 同時接続数 | 不要 |
| `apple_rss` | 14か国 × 4フィード(有料/無料アプリ・有料書籍・楽曲) 1,224件 | 不要 |
| `google_trends_rss` | 検索需要(日/米/英/独/韓) | 不要 |
| `wikimedia_pageviews` | 関心量の絶対PV値(日/英) | 不要 |
| `estat_dashboard` | 都道府県別 有効/新規求人倍率(月次) | 不要 |
| `remotive` / `remoteok` | 企業が求めるスキルの分布 | 不要 |
| `npm_downloads` / `pypi_downloads` | 技術の実採用量 | 不要 |
| `indeed_hiring_lab` | AI関連求人比率・求人数指数(国別) | 不要 |

---

## 3. PCで始める手順

### 3-1. リポジトリを取得する

```bash
git clone https://github.com/akiijauto/self-evolving-ai-public.git
cd self-evolving-ai
git checkout claude/global-demand-data-system-1z8z66
git pull
```

すでにclone済みなら `git pull` だけでよい。**未プッシュの変更は残っていない**
(引き継ぎ時点でリモートと完全に一致している)。

### 3-2. 動作確認

Python 3.8以上があれば動く。**外部ライブラリのインストールは不要**
(標準ライブラリのみで書いてある)。

```bash
# テスト(通信しない、数秒で終わる)
python -m unittest discover -s tests

# 何が実行され、何がなぜスキップされるかを確認する(通信しない)
python scripts/demand_collect.py --date 2026-08-04 --dry-run
```

Windows のコマンドプロンプトでも文字化けしないよう、全スクリプトで
UTF-8出力を設定済み。POSIX依存の絶対パスも使っていない。

### 3-3. 実際に収集する

```bash
# 収集(承認済みの全収集元。10〜15分程度かかる)
python scripts/demand_collect.py --date 2026-08-04

# Notionに貼れるMarkdownを生成
python scripts/demand_report.py --date 2026-08-04

# 蓄積データから判断材料を抽出
python scripts/demand_insight.py --date 2026-08-04
```

`--date` は必ず明示する(スクリプト側で現在日時を自動取得しない設計。
呼び出し側のAIが把握している日付を渡す、というこのリポジトリ共通のルール)。

### 3-4. 新しい収集元を検討するとき

```bash
# robots.txt を実際に取得して許可状況を確認する
python scripts/demand_check_robots.py --url https://example.com/ranking --date 2026-08-04
```

**robots.txt だけで判断してはいけない。** 利用規約も必ず確認する。
Amazon は robots.txt がベストセラーを禁止していないが規約が禁止しており、
価格.com は robots.txt が許可的だが規約が「保管する行為」まで禁止している。

---

## 4. あなたの判断待ちの項目(4件)

このセッションでは、あなたの意思決定が必要なため**意図的に手を付けていない**
ものが4つある。

### (1) Notion Integration Token

GitHub Actions から Notion へ自動反映するために必要。発行してGitHub Secretsに
登録すれば、翌朝から自動で子ページが作られる。

- 必要なSecret: `NOTION_TOKEN` と `NOTION_PARENT_PAGE_ID`
- 親ページID: `3b024f4cf1af81709659eab8458a3096`(「🌏 グローバル需要データ」)
- 未登録でもMarkdown生成とコミットまでは動く(Notion反映だけがスキップされる)

### (2) Netflix Top 10 の採否

- robots.txt が **ClaudeBot を明示的に許可グループへ列挙**している
- 公式にダウンロードボタン付きで TSV/XLSX を配布(90か国 × 週次、視聴数と視聴時間)
- **ただし利用規約1.8条**が「the Netflix service」へのロボットアクセスと
  データマイニングを禁止しており、その定義が Tudum の公開データ配布ページに
  及ぶか文言上不明確
- 判断が付かないため `investigating` のまま。承認すれば収集できる

### (3) Tier 3 収集元の有効化(14件)

robots.txt は許可しているが、規約が未確認またはグレーなもの。
`--allow-tier3` を付けても `status` が `approved` でなければ動かない。

`coconala`(最優先とご指示のあったもの) / `tohan` / `kinokuniya` / `dlsite` /
`oricon` / `cosme_net` / `aupay_market` / `lazada` / `hktvmall` / `noon` /
`musinsa` / `11st` / `momo` / `hellowork_public`

特筆:
- **`lazada` `hktvmall` `noon` `musinsa`** は robots.txt が **ClaudeBot や
  anthropic-ai を名指しで許可**している(HKTVmallは `# Welcome crawlers: AI training`
  とコメントまで書いている)
- **`oricon` は利用規約ページが存在しない**(6つのURLを試して全て404/403)。
  規約に違反しないことを確認できないため、収集は推奨しない
- **`coconala`** は規約第14条第1項で集計データの権利が同社に帰属するため、
  取得しても二次利用・公表はできない(非公開の分析に限る)

### (4) 要認証APIの取得(10件)

`rakuten_ranking_api`(**要 applicationId + accessKey**。無料アプリIDのみでは
使えないことを実測確認済み) / `yahoo_shopping_api` / `estat`(appId) /
`nyt_books_api` / `aliexpress_affiliate` / `coupang_partners` /
`flipkart_affiliate`(新規登録停止中) / `bestbuy_api` / `kyujinbox_api` /
`hellowork_api`(職業紹介事業の許可が必要)

---

## 5. 規約上「使わない」と決めたもの(40件)

`demand/sources.json` に理由を全文で記録してある。主なもの:

Amazon(6か国) / eBay / Walmart / Target / Etsy / Gumroad / Creative Market /
Billboard / IMDb / Spotify / NYT本体 / Docker Hub / G2 / Pinterest /
淘宝・天猫 / JD / 拼多多 / 小紅書 / 1688 / 抖音 / Shopee / Temu / TikTok Shop /
Naver / PChome / 楽天HTML / ZOZO / ヨドバシ / honto / 駿河屋 / pixiv・BOOTH /
日経 / 価格.com / クラウドワークス / SKIMA / ランサーズ / Wantedly / LinkedIn /
Indeed本体 / スタンバイ / Reddit

**運用方針として守ってほしいこと**: robots.txt が ClaudeBot / GPTBot 等の
AIクローラーを拒否しているサイトは、User-Agent を変えれば技術的に取得できても
`rejected` として扱う。UAを変えて拒否を回避する行為はサイト運営者の意思表示に
反するため。この方針は `sources.json` の `_readme` にも明記してある。

---

## 6. 壊さないでほしい設計上の約束

1. **規約ガードを迂回しない。** `registry.check()` を通さずに収集しない。
   Tier 4(規約違反)は `--allow-tier3` を付けても実行されない構造になっている。
2. **収集元をまたいだ合算ランキングを作らない。** 収集元ごとに算出基準
   (販売数/閲覧数/掲載数)が異なり、その基準は多くの場合非公開。合算すると
   どの実態も表さない数字になる。国が違うランキングも混ぜない。
3. **スキップした収集元は理由付きで必ず表に出す。** 黙って対象を減らすと
   「毎日動いている」ことをもって正常だと誤認する。
4. **収集は冪等に保つ。** `store.replace_source()` を使う。追記だと同じ日に
   2回実行したときに順位が重複し、しかも例外が出ないため気づけない。
5. **解釈を誤ると危険な指標には `category_caveats` を付ける。** レポートの表の
   直前に警告が自動で出る。注記を人間の記憶に委ねると数字だけが独り歩きする。

---

## 7. つまずきやすい点(実際に踏んだもの)

| 事象 | 実際に起きたこと |
|---|---|
| PyPI が 429 で失敗する | `pypistats.org` はIPベースのレート制限が厳しい。3秒間隔＋バックオフを入れてあるが、共有IP環境では全滅することがある。個別パッケージの失敗は許容し、全滅時のみエラーになる設計 |
| Apple の `top-grossing` | **存在しない**。調査で「ある」と報告されたが、limitと国を変えて検証した結果すべてHTTP 404。使えるのは `top-paid`(ダウンロード数ベース) |
| 楽天ランキングAPI | 「無料アプリIDのみで使える」は**誤り**。2026年のインフラ移行で `accessKey` が必須になった |
| Indeed Hiring Lab のAI求人比率 | **AIスキル需要の指標として読んではいけない。** カナダの急騰は2026年1月のオンタリオ州のAI開示義務化による制度要因で、Hiring Lab 自身が「この指標は変質した」と明言している。レポートに警告が自動表示される |
| Google トレンド / Wikimedia | 収集件数は多い(150件)が、商業的な語を含むものは**0件**。受託・人材需要の把握には寄与していない。`demand_insight.py` で可視化してある |

---

## 8. 次にやるとよさそうなこと

優先度順。いずれも着手前に上記「判断待ち」の決着が必要なものがある。

1. **Notionトークンを登録して自動反映を有効化する**(判断待ち(1))。
   これが済むと毎朝7:10に子ページが自動生成され、手作業がゼロになる
2. **数日〜2週間データを貯める。** 現在2日分しかなく、順位変動の傾向は
   まだ読めない。システムが自前で傾向を語れるのは最短で2週間後
3. **未実装の収集元を実装する。** 規約はクリアだが収集モジュールが無いもの:
   `us_census_marts`(米国小売統計)、`jada`(自販連の車名別登録台数。Excel解析が
   必要)、`stackoverflow_survey`
4. **`demand_insight.py` の切り口を増やす。** 現在は地域の需給構造・求人ボード
   間の食い違い・トレンドの商業シグナルの3つ

---

## 9. このセッションで残っている宿題

- **PR #8 は draft のまま未マージ。** マージするかどうかは未決
- **クラウドセッション側のPR定期チェックは停止済み**(2026-08-03)。
  自動で起動することはないので、PC側で自由に進めてよい
- **ビジネス分析の結果をNotionに保存していない。** 「AIエージェントは
  利用中3.3%に対し関心あり49.3%」など、調査で得た示唆をまとめた内容が
  チャット上にしか無い状態
