# ローカルPCコンテキストバックアップ機能 設計検討

本ドキュメントは「ローカルPC上にしか存在しないコンテキストをどう保全し、
どうAI/スマホから参照可能にするか」の検討結果をまとめたものである。

関連: [要件定義.md](./要件定義.md) の「Phase 4」

## 0. 決定事項(2026-07-31)

| 項目 | 決定 | 備考 |
|---|---|---|
| クラウドストレージ | **Google Drive** | Google Drive for desktop の同期フォルダへコピーする |
| Obsidian | **現時点では導入しない**(将来導入可) | 下記「Obsidianの扱い」参照 |
| バックアップ対象パス | 検出結果をもとに確定する | `backup_manifest.py --detect` で実データから決める |
| Notion連携 | **保留**(後日判断) | APIキーが必要。保留中はフェーズA・B・Dだけで完結する |
| 実行頻度 | 未定 | まず手動、安定後に日次ルーティンへ相乗り |

Notion連携(フェーズC)を保留にしても、バックアップとしての機能
(消失リスクの解消)はフェーズA・B・Dだけで成立する。フェーズCが担うのは
「スマホからの参照性」のみであり、保全とは独立している。

### Google Drive for desktop が未導入の場合

クラウドへの保存にはGoogle Drive for desktopのインストールが必要
(Webブラウザ版だけでは、スクリプトからコピーできるローカルフォルダが存在しない)。

未導入の間も、**保存先を別ドライブ・外付けディスクにすれば今日からバックアップは取れる**。
`--dest` を変えるだけでスクリプトの動作は同じ。

```bash
python scripts/backup_run.py --date <日付> --dest "D:/context-backup"
```

ただし同一PC内のドライブだけでは、PC本体の故障・盗難・ランサムウェアで
正本と一緒に失われる。オフサイト(=Google Drive)は結局必要なので、
これはあくまでインストールまでの暫定運用と位置づける。

段階としては次の順序になる。

1. **今すぐ**: 外付け/別ドライブへバックアップ(3-2-1の「2媒体」を確保)
2. **Drive導入後**: Google Driveも保存先に追加(「1オフサイト」を確保)

### 保存先の構成: マイドライブのミラーリング vs 「パソコン」区画

Google Drive for desktop には、方向の違う2つの機能がある。どちらを使うかは
**マイドライブの使用量**で決まる。

| 機能 | 方向 | 向いている条件 |
|---|---|---|
| マイドライブのミラーリング | クラウド → ローカル | マイドライブが小さい(数GB以下) |
| 「パソコン」区画のバックアップ | ローカル → クラウド | マイドライブが大きい / 余計な物を落としたくない |

ミラーリングは**マイドライブ全体をローカルにダウンロードする**(選択的な同期はできない)。
マイドライブに数百GBある環境では、バックアップのために無関係な大量のデータを
ローカルへ落とすことになり、割に合わない。

その場合は方向を逆にする。ローカルにバックアップ先フォルダを作って
`backup_sync.py` はそこへ書き、そのフォルダを Drive の「パソコン」区画に
登録してクラウドへ上げる。

```
正本(ローカル) --backup_sync.py--> ローカルのバックアップ先 --Driveの「パソコン」--> クラウド
```

この構成の利点:

- マイドライブから何もダウンロードしない(ディスクを消費しない)。
- バックアップ先がローカルにあるので、ハッシュ照合(`backup_verify.py`)が
  ネットワーク越しにならず速い。
- 結果として「ローカルの正本」「ローカルの複製」「クラウドの複製」の3コピーになる。

注意: 「パソコン」区画は削除も同期される(アーカイブではない)。
ただし `backup_sync.py` は削除を一切行わないため、この経路で意図しない削除は発生しない。

### Google Drive を使う上での必須設定

Google Drive for desktop には「ストリーミング」と「ミラーリング」の2モードがある。
本機能では **ミラーリングを選ぶ**。

- ストリーミング(既定): ファイルの実体はクラウドにあり、ローカルはプレースホルダ。
  この状態だとバックアップとして機能しない(PCがオフライン/障害時に読めない)上、
  スクリプトがハッシュ計算のためにファイルを開くたびに実ダウンロードが走る。
- ミラーリング: ローカルとクラウドの両方に実体を持つ。**3-2-1の「2媒体」がこれで成立する。**

### Obsidianの扱い

Obsidianは個人利用は無料(有料なのは Obsidian Sync / Publish であり、本設計では
Sync を使わないため課金は発生しない)。ただし **今は導入不要**と判断する。

Obsidian の Vault は「Markdownファイルが入ったただのフォルダ」であり、
後からいつでも既存フォルダを Vault として開ける。したがって、

- いま決めるべきは「ノートをプレーンMarkdownで1箇所にまとめる」ことだけ。
- Obsidianが必要になった時点(リンク・グラフ・検索が欲しくなった時)に、
  そのフォルダを Vault として開けばよい。移行コストはゼロ。

先にObsidianを入れると、`.obsidian/` 配下の設定・ワークスペース・プラグインが
バックアップ対象に混ざり、実質的な中身より設定ファイルの差分の方が
頻繁に動くことになる(`.obsidian/workspace.json` はペインを動かすたびに更新される)。
導入する場合は `.obsidian/workspace*.json` を除外対象に加えること。

## 1. 背景と課題

`self-evolving-ai` は「同じ場所に同じ形式でコンテキストが蓄積される」ことを目指しており、
このリポジトリ配下(`context/`)についてはgit + GitHubで多重化されている。
一方、実際の開発コンテキストの多くはローカルPCにしか存在しない。

現状の穴:

| 対象 | 置き場所 | 現在の保全状況 |
|---|---|---|
| `context/` 配下(イベント・振り返り・判断基準) | このリポジトリ | git + GitHubで保全済み |
| `CLAUDE.md` / `AGENT.MD` 相当 | `C:/Users/<user>/AI開発/` 等 | ローカルのみ(消えたら復元不能) |
| スキル定義 / MCP設定 / 各種設定ファイル | `~/.claude/` 等 | ローカルのみ |
| ノート(Obsidian Vault 等) | ローカルのみ | ローカルのみ |
| プロジェクト作業ファイル | ローカル各所 | 一部のみgit管理 |

課題は2つある。

1. **消失リスク**: PC故障・OS再インストール・誤削除で、AIに渡す前提知識が丸ごと消える。
2. **参照性**: 外出先(スマホ)から自分のコンテキストを確認できない。

## 2. 方式の比較検討

事前調査(Obsidian Sync vs Python自動化)の結論を踏まえ、4方式を比較した。

| 方式 | AIから読めるか | 競合(コンフリクト) | 秘密情報の制御 | 大容量ファイル | 評価 |
|---|---|---|---|---|---|
| A. Obsidian Sync等の専用同期 | ✕(E2E暗号化でクラウド側は不可読) | 自動解決されるが不透明 | ✕(何が上がるか制御しづらい) | ○ | 不採用 |
| B. gitリポジトリのみ | ◎ | ○(gitの通常運用) | ○(.gitignore) | ✕(バイナリ/巨大ファイルに弱い) | 部分採用 |
| C. クラウドストレージ同期フォルダ + Python前処理 | ◎ | △(スマホから書くと発生) | ◎(スクリプトで明示的に選別) | ◎ | 採用 |
| D. C + Notionをメタデータ/タグDBのハブにする | ◎ | ◎(スマホをRead-Onlyに割り切ると消滅) | ◎ | ◎ | 採用(後段フェーズ) |

**方式Aを採らない理由**: 同期プロセスが完全にブラックボックス化され、E2E暗号化のため
クラウド上のデータをAIに読ませることが構造上できない。本プロジェクトの目的は
「AIが読めるコンテキスト置き場」であり、この時点で要件と衝突する。

**方式Cを採る理由**: 同期プロセス自体をPythonスクリプトとして持つと、同期の"前"に
処理を挟み込める。具体的には、クラウドへ保存する前にローカルのMarkdownを読み込み、
要約・タグ付け・メタデータ整形を行ってから保存する、というパイプラインが組める。
既存スクリプト群(標準ライブラリのみ・CLI起動)と同じ流儀でそのまま拡張できる。

**方式Dを採る理由**: 「スマホからは閲覧(Read-Only)できれば十分」と割り切ることで、
自前同期スクリプトの最大の障壁だった「モバイル側でのファイル競合・更新の手間」が
完全に消える。実データはクラウドストレージに置き、Notionは
「タイトル・タグ・更新日・実体へのURL」だけを持つラベルDBに徹する。

## 3. 採用アーキテクチャ

```
  [ローカルPC]                        [スクリプト]                      [保存先]

  AI開発/CLAUDE.md          ┐
  ~/.claude/skills/         │   backup_manifest.py             ┌─→ ① クラウドストレージ
  Obsidian Vault/           ├─→  (走査・除外・ハッシュ)   ─→   │     実データ(正本のコピー)
  各プロジェクトのメモ       │        ↓ manifest.jsonl          │
  self-evolving-ai/context/ ┘   backup_sync.py                 ├─→ ② Notion ラベルDB
                                 (コピー・更新のみ)             │     メタデータ+タグ+URL
                                     ↓                          │
                                backup_to_notion.py            └─→ ③ git(テキストのみ)
                                 (メタデータのupsert)                差分履歴・監査用

  [スマホ] ──検索──→ ② Notion ──URLを開く──→ ① クラウドストレージ(閲覧のみ)
```

役割分担:

- **① クラウドストレージ**: 実データの置き場。既存の同期フォルダ
  (OneDrive / Google Drive / Dropbox のいずれか)にコピーするだけなので、
  新規のAPIキー取得が不要。
- **② Notion**: 検索・タグ用のインデックス。実データは持たず、URLで①を指す。
  スマホからの入口はここ一本に絞る。
- **③ git**: テキスト系(Markdown/JSONL)のみ、差分と履歴を残す監査用。
  巨大ファイル・バイナリは載せない。

3系統に置くことで、結果的に3-2-1バックアップ(3コピー・2媒体・1オフサイト)を満たす。

## 3.5 正本(single source of truth)の決め方

同じ内容が3箇所(ローカルPC / Google Drive / git)に存在するため、
「どれが正本か」が曖昧だと、古い方を編集して作業を失う事故が起きる。
そこで**1つのファイルの正本は必ず1箇所**と定め、それを機械的に保証する。

### ルール

| 種類 | 正本 | 他の場所の位置づけ |
|---|---|---|
| `context/` 配下(イベント・振り返り・判断基準) | **git(このリポジトリ)** | Driveへはコピーしない(二重管理を避ける) |
| ローカルPCのAI設定・ノート | **ローカルPC** | Driveは複製。編集しない |
| Google Drive上のファイル | 正本にならない | 常に複製。閲覧と復元元にのみ使う |

つまり **Google Drive側は絶対に編集しない**。編集は必ずローカルPCで行う。

### ルールを「守れているか」機械的に確かめる仕組み

運用ルールを書いただけでは必ず破られるため、3段構えで担保する。

1. **一方向コピーしか実装しない**
   `backup_sync.py` は 正本 → バックアップ先 の方向しか持たない。
   逆方向の同期機能を作らないことで、Driveの内容がローカルを上書きする経路が存在しない。
   復元が必要なときだけ、人間が明示的にコピーする。

2. **バックアップ先にマーカーファイルを置く**
   同期時に `_このフォルダはバックアップです.md` を自動生成し、
   「編集しないこと」と**正本の絶対パス**を書く。スマホやWebでフォルダを開いた人にも、
   そこが複製であることが一目で分かる。

3. **バックアップ先の編集を検知して、黙って上書きしない**
   「バックアップ先がローカルと違う」理由は2通りある。
   - (a) 正本が更新された → 上書きしてよい
   - (b) バックアップ先が直接編集された → 上書きすると編集が消える

   `_backup_state.json` に「前回書き込んだ内容のハッシュ」を記録しておくことで、
   この2つを区別する。(b) は**要確認としてスキップ**し、レポートに出した上で
   終了コード1を返す。上書きするには `--force-overwrite` の明示が必要。

   これがないと、Drive側で書き足した内容が次回の同期で静かに消える。
   「正本が曖昧」であることの実害はここに出るため、検知を実装で担保している。

## 4. バックアップ対象の階層(Tier)

すべてを一律に扱うと、秘密情報の流出と容量肥大の両方を招く。Tierで扱いを分ける。

| Tier | 内容 | クラウド | Notion登録 | git |
|---|---|---|---|---|
| A | AI設定の正本(CLAUDE.md / AGENT.MD / スキル定義 / 判断基準) | ○ | ○ | ○ |
| B | ノート・振り返り・設計メモ(Markdown中心) | ○ | ○ | ○(サイズ次第) |
| C | プロジェクト作業ファイル(ソース以外の成果物・画像等) | ○ | ○(メタのみ) | ✕ |
| X | 除外(下記) | ✕ | ✕ | ✕ |

**Tier X(常に除外)** — スクリプト側にハードコードする既定の除外リスト:

- 認証情報: `.env` / `*.key` / `*.pem` / `credentials*` / `*token*` / `.aws/` / `.ssh/`
- 再生成可能: `node_modules/` / `.venv/` / `__pycache__/` / `dist/` / `build/` / `.next/`
- VCS内部: `.git/`(リポジトリ自体はgit側で保全されるため二重に運ばない)
- サイズ超過: 既定 50MB を超えるファイル(manifestには「除外した」記録だけ残す)

除外は「消し忘れ」ではなく設計上の要件として扱う。Tier Xに該当するパターンを
検出した場合、スクリプトは黙って飛ばすのではなく件数をレポートに出す。

### 4.1 推奨する対象パス(Windows想定)

`backup_manifest.py --detect` が存在確認する候補。実際に何があるかは検出結果で決める。

| パス | Tier | 入れる理由 |
|---|---|---|
| `~/AI開発/` | A | CLAUDE.md等、AI設定の正本。**最優先**(消えると復元不能) |
| `~/.claude/CLAUDE.md` | A | ユーザーグローバルの指示ファイル |
| `~/.claude/settings.json` | A | 権限・環境変数・フック設定 |
| `~/.claude/skills/` | A | 自作スキル。手で書いた資産で再生成できない |
| `~/.claude/commands/` | A | 自作スラッシュコマンド |
| `~/.claude/agents/` | A | サブエージェント定義 |
| `~/.codex/` `~/AGENTS.md` | A | GPT Codex側の設定(ツール非依存という方針上、両方揃えて意味がある) |
| ノート置き場(1箇所に集約する) | B | 設計メモ・振り返り。Markdownで統一する |
| `~/Documents/` の一部 | B | 上記に集約できていない既存メモ |

**含めないもの**(それぞれ理由が違う):

- `~/.claude/projects/` — 全セッションの会話ログ。**最も情報量が多いが最も扱いが難しい**。
  数百MB〜GB級に育つ上、貼り付けたトークンやパスがそのまま残っている。
  含めるなら Tier B として、直近90日分のみ・サイズ上限付き・
  秘密パターン検査を通したものに限る(初期フェーズでは対象外にする)。
- `~/.claude.json` — MCPサーバー設定。APIキーが平文で入りうるため常時除外(Tier X)。
- `~/.claude/.credentials.json` — 認証トークンそのもの。常時除外(Tier X)。
- gitリポジトリの作業ツリー — GitHubに push されている限り二重管理になる。
  ただし**pushされていないローカルブランチは保全されない**ので、
  「未pushのリポジトリ一覧」を別途レポートする方が費用対効果が高い(将来の拡張候補)。
- Google Driveの同期フォルダ自体 — 走査対象に含めるとコピー済みのものを
  再度対象として拾うため、スクリプト側で明示的に除外する。

## 5. データモデル

### 5.1 manifest.jsonl(既存フォーマット規約に準拠: 1行1JSON・追記/再生成)

```json
{"backup_at": "2026-07-31", "tier": "A", "rel_path": "AI開発/CLAUDE.md", "src": "C:/Users/<user>/AI開発/CLAUDE.md", "size": 8123, "sha256": "…", "mtime": "2026-07-30T22:10:03", "tags": ["ai-config", "claude"], "status": "included"}
{"backup_at": "2026-07-31", "tier": "X", "rel_path": "proj/.env", "size": 220, "status": "excluded", "reason": "secret-pattern"}
```

必須キー: `backup_at` / `tier` / `rel_path` / `status`。
`status` は `included` / `excluded` / `unchanged` の3値。
`sha256` を持つことで、コピー先との照合(=復元可能性の検証)が後から自動でできる。

### 5.2 Notion ラベルDBのプロパティ

| プロパティ | 型 | 由来 |
|---|---|---|
| Name | title | ファイル名 |
| Path | text | `rel_path` |
| Tags | multi-select | 自動付与(ディレクトリ名 + 拡張子 + 任意でAI生成タグ) |
| Tier | select | A / B / C |
| Updated | date | `mtime` |
| Size | number | `size` |
| Hash | text | `sha256`(重複検出・照合用) |
| URL | url | クラウドストレージ上の共有リンク |

Notionは「検索してURLへ飛ぶ」ためだけに使う。ここに本文を持たせない
(持たせると二重管理になり、どちらが正本か分からなくなる)。

## 6. スクリプト構成(実装計画)

既存スクリプトと同じ流儀に揃える。標準ライブラリのみ、`python scripts/xxx.py` で起動、
破壊的操作は既定dry-run + `--apply` 必須。

| スクリプト | 役割 | 外部依存 | 危険度 |
|---|---|---|---|
| `scripts/backup_run.py` **(実装済み)** | 上記を順に呼び出す入口。対象パスとバックアップ先を自動検出し、既定は書き込みなしの確認 | なし | 低〜中(`--apply`時のみ書き込み) |
| `scripts/backup_manifest.py` **(実装済み)** | 対象パスを走査し、Tier判定・除外判定・ハッシュ計算をして `manifest.jsonl` を生成。`--detect` で候補パスの存在確認のみも可能 | なし | 低(読み取り専用) |
| `scripts/backup_sync.py` **(実装済み)** | manifestに従って同期先ディレクトリへコピー。既定dry-run、`--apply` で実行 | なし | 中(書き込み) |
| `scripts/backup_verify.py` **(実装済み)** | 同期先のファイルを再ハッシュし、manifestと突き合わせて欠損・破損を検出。異常時は終了コード1 | なし | 低(読み取り専用) |
| `scripts/backup_restore.py` **(実装済み)** | バックアップから別ディレクトリへ復元し、sha256照合まで行う。正本へは書き戻さない | なし | 中(書き込み) |
| `scripts/register_backup_task.ps1` **(実装済み)** | Windowsタスクスケジューラへ定期実行を登録/解除 | なし | 低 |
| `scripts/backup_to_notion.py` *(保留)* | manifestをNotion DBへupsert | **Notion APIキー** | 中(外部送信) |

対象を複数指定したとき同名の相対パス(例: `notes/memo.md`)が衝突しないよう、
同期先では対象ごとにラベルの階層を1つ挟む(`<ラベル>/<相対パス>`)。
ラベルは対象パスの末尾名から生成し、重複した場合は連番を付けて区別する。

対象パスの設定は、秘密情報を含みうるローカル絶対パスをリポジトリに
コミットしないよう、`--source` の複数指定 + 任意の設定ファイル
(`.gitignore` 対象)で渡す。

### 安全設計(既存の運用ルールを踏襲)

- **削除は一切しない**。同期先に余分なファイルがあっても消さず、レポートに出すだけ。
  ミラー削除は事故時の被害が大きく、バックアップの目的と相反する。
- **秘密情報の検出時は中断**。Tier X のパターンに一致したファイルが
  `--apply` 対象に混入した場合、コピーせず異常終了する。
- **Notion連携は新規APIキーが必要**なため、着手前にユーザー確認が必須
  (運用ルールの「APIキー・認証情報」カテゴリに該当。アイデア7の保留と同じ理由)。
- **上書き前に必ずハッシュ比較**し、同一なら書き込まない(`status: unchanged`)。

## 7. 段階導入計画

| フェーズ | 内容 | 新規の認証情報 | 状態 |
|---|---|---|---|
| A | `backup_manifest.py`(走査・Tier判定・除外・ハッシュ)= 読み取り専用 | 不要 | **実装済み** |
| B | `backup_sync.py` + `backup_verify.py`。ローカルへコピー→Driveの「パソコン」区画でクラウドへ | 不要 | **完了**(2026-08-03 初回実行・検証済み) |
| C | `backup_to_notion.py`。スマホからのRead-Only参照導線 | **必要(Notion API)** | **保留**(後日判断) |
| D | 定期実行の登録 + 復元スクリプト・手順書(リストア訓練) | 不要 | **実装済み**(初回の訓練は未実施) |

フェーズAは「何がバックアップ対象になるのか」を一覧で可視化するだけでも価値があり、
かつ読み取り専用で事故らないため、最初に着手する単位として適切だった。

## 8. 未確定事項(着手前に確認したいこと)

- [x] **クラウドストレージ**: Google Drive に決定(ミラーリング設定が前提)。
- [x] **Obsidian**: 現時点では導入しない(後から無料でVault化できるため急がない)。
- [x] **Notion連携をやるか**: 保留。フェーズA・B・Dだけでバックアップとしては完結する。
- [ ] **バックアップ対象のトップレベルパス**: `--detect` の結果を見て確定する。
- [ ] **同期先パス**: Google Driveのローカルフォルダ(例: `G:/マイドライブ/context-backup/`)。
- [ ] **実行頻度**: 手動実行か、既存の日次ルーティンに相乗りするか。

スクリプトは実装済みなので、残り2つ(対象パス・同期先パス)は実行時の引数として
渡すだけでよく、コードの変更は不要。

## 9. 成功条件

- [x] `backup_manifest.py` を実行すると、対象・除外・Tierの内訳がレポートとして出る。
- [x] 秘密情報パターン(`.env` 等)が確実に除外され、レポートに件数が明示される。
- [x] `backup_sync.py` が既定dry-runで動き、`--apply` を付けたときだけコピーする。
- [x] 同期先に余分なファイルがあっても削除せず、報告だけする。
- [x] 認証情報がコピー対象に混入していた場合、1件でもあればコピーせず中断する。
- [x] `backup_verify.py` でコピー先のハッシュ照合が通り、「復元できる状態か」が
      機械的に判定できる(欠損・改変を検出したら終了コード1)。
- [x] 実PC(Windows)で初回の manifest → dry-run → apply → verify を通す
      (2026-08-03、1433件 / 24.9MB、全件一致)。
- [ ] (フェーズC・保留) スマホのNotionからファイルを検索し、URL経由で実データを閲覧できる。
- [x] (フェーズD) 復元スクリプト `backup_restore.py` と [復元手順.md](./復元手順.md) を用意した。
- [x] (フェーズD) 定期実行を `scripts/register_backup_task.ps1` で登録できるようにした。
- [ ] (フェーズD) 実PCで復元訓練を1回行う。

## 10. 初回実行の実績(2026-08-03)

| 項目 | 値 |
|---|---|
| 対象 | `~/AI開発`(936件) / `~/.claude/settings.json` / `~/.claude/plugins`(426件) / `~/.codex`(71件) |
| 採用 | 1433件 / 24.9MB |
| 除外(認証情報) | 24件(中身を読まずに除外) |
| バックアップ先 | `C:\context-backup` → Google Driveの「マイ パソコン」経由でクラウドへ |
| 検証結果 | 全件一致(欠損0 / 不一致0) |

除外パターンの調整で **7253件 / 192.4MB → 1433件 / 24.9MB(約87%減)** になった。
削れたのはプラグインのキャッシュ・実行ファイル・モデルデータ・ローカル作業用DBで、
いずれもマーケットプレイスや配布元から再取得できるもの。

この過程で得た教訓: **除外パターンは推測で書くと外れる。**
最初に書いた `.codex` 向けの除外(セッションログ想定)はほぼ効かず、
実際の中身はプラグインキャッシュだった。棚卸しレポートの「容量上位20件」を
先に読んでから判断すべきだった(`critic_findings.jsonl` に記録済み)。
